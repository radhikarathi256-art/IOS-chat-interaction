// Secrets.example.swift — the template that IS tracked by git.
//
// Setup: copy this file to `Secrets.swift` in the same folder and paste in the
// real values. `Secrets.swift` is gitignored, so the credentials never leave
// your machine. Xcode auto-syncs this folder, so no project settings change.
//
//     cp Secrets.example.swift Secrets.swift
//
// This file must NOT declare the constants itself, or it would collide with
// the real Secrets.swift at compile time — hence everything below is commented.

import Foundation

// let SUPABASE_HOST = "your-project-ref.supabase.co"
// let SUPABASE_KEY  = "your-publishable-anon-key"
